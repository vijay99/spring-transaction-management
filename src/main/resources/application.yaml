spring:
  datasoruce:
    driver-class-name:
    url:  jdbc:postgresql://localhost:5432/demo
    username: postgres
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port:9999


